{
	"$schema": "https://json-schema.org/draft/2020-12/schema#",
	"$id": "http://example.com/schemas/nuevo.schema.json",
	"type": "object",
	"title": "Super heroes",
	"description": "Superheroes description",
	"required": ["squadname", "hometown", "formed", "secretBase", "active","members"],
	"properties": {
		"squadname": {
			"type": "string",
			"description": "Squad name"
			},
        "hometown": {
          	"type": "string",
            "description": "Home town"
        },
        "formed": {
          	"type": "integer",
            "description": "Year the squad was formed",
            "minimum": 0
        },
        "secretBase": {
          	"type": "string",
            "description": "Name of their secret base"
        },
        "active": {
         	"type": "boolean",
            "description": "Whether the squad is active or not"
        },
        "members": { "$ref": "#/$defs/members" }
    },
	"$defs": {
		"members": {
			"type": "array",
            "items": {
              	"type": "object",
                "properties": {
                  "name": {
                    "type": "string",
                    "description": "Name of the superheroe"
                  },
                  "age": {
                    "type": "integer",
                    "minimum" : 0,
                    "description": "Superheroes' age"
                  },
                  "secretIdentity": {
                    "type": "string",
                    "description": "Real name of the superheroe"
                  },
                  "powers": {
                    "type": "array",
                    "description": "List of powers of the superheroe",
                    "minItems" : 1,
                    "uniqueItems": true,
                    "items": {
                      "type": "string"
                    }
                  }
                },
                "required": ["name", "age", "secretIdentity", "powers"]
            },
			"uniqueItems" : true,
            "minItems": 1
		}	
	}
}